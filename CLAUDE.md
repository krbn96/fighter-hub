# Project

FIGHTER HUBは格闘ゲーム向けのチーム作成・メンバー募集Webアプリケーション。

Tech Stack:
- Java 21
- Spring Boot 4.0.8
- PostgreSQL 18
- Maven
- Spring Data JPA
- Spring Security
- Springdoc OpenAPI

# Architecture

Controller -> Service -> Repository -> PostgreSQL

既存のレイヤードアーキテクチャとパッケージ構成を維持する。

# Coding Rules

- constructor injectionを使用する
- field injectionは使用しない
- JPA EntityをAPIレスポンスとして直接返さない
- DTOを使用する
- DTOは既存実装に合わせてrecordを優先する
- Entityに不要なsetterを作らない
- トランザクション境界はService層に置く
- 読み取り処理では@Transactional(readOnly = true)を使用する
- OSIVは無効のまま維持する
- 既存コードの命名・設計パターンを優先する
- ControllerにはSpringdoc/OpenAPIアノテーションを付ける
- Service TestとController Testを作成する
- テストメソッド名は既存テストの日本語命名パターンに合わせる
- 実装完了後はMavenの全テストを実行する

# API / Database Rules

- API仕様を推測して変更しない
- DB仕様を推測して変更しない
- docs/openapi.yamlは未実装APIの設計資料として参照する
- 実装済みAPIについては実装コードを優先する
- 仕様が曖昧または矛盾している場合は、勝手に判断せず質問する

# User Design

Userテーブル名はT_USERS。

使用キャラクター情報について別のUserCharactersテーブルは作成しない。

T_USERSに以下の形式で保持する。

- character_1 ～ character_4
- rank_1 ～ rank_4
- mr_1 ～ mr_4

その他に以下のプロフィール情報を持つ。

- play_time_start
- play_time_end
- message

emailはDB上NULLを許容する。
これは将来的なSNSログイン対応を考慮したもの。

ただし現在設計されている通常ユーザー登録
POST /usersではemailを必須とする。

## T_USERS Columns

- id: BIGINT, PK, NOT NULL。JPAでは@GeneratedValue(strategy = GenerationType.IDENTITY)を使用する（Hibernate/PostgreSQLが生成するDDLはGENERATED BY DEFAULT AS IDENTITYになる。これを許容する）
- name: VARCHAR(255), NOT NULL

- character_1: BIGINT, nullable, FK -> M_CHARACTERS.id
- rank_1: VARCHAR(30), nullable
- mr_1: INTEGER, nullable

- character_2: BIGINT, nullable, FK -> M_CHARACTERS.id
- rank_2: VARCHAR(30), nullable
- mr_2: INTEGER, nullable

- character_3: BIGINT, nullable, FK -> M_CHARACTERS.id
- rank_3: VARCHAR(30), nullable
- mr_3: INTEGER, nullable

- character_4: BIGINT, nullable, FK -> M_CHARACTERS.id
- rank_4: VARCHAR(30), nullable
- mr_4: INTEGER, nullable

- play_time_start: TIME, nullable
- play_time_end: TIME, nullable
- message: TEXT, nullable
- email: VARCHAR(255), nullable
- x_id: VARCHAR(255), nullable
- discord_id: VARCHAR(255), nullable
- password_hash: VARCHAR(255), nullable

- created_at: TIMESTAMP, NOT NULL。DB DEFAULT CURRENT_TIMESTAMPには依存せず、Hibernateの@CreationTimestampで管理する
- updated_at: TIMESTAMP, NOT NULL。DB DEFAULT CURRENT_TIMESTAMPには依存せず、Hibernateの@UpdateTimestampで管理する
- delete_flag: BOOLEAN, NOT NULL。DB DEFAULT FALSEには依存せず、Java Entity側で初期値falseを設定する（boolean deleteFlag = false）

## T_USERS Notes

- 以前のDB設計ではe_mailという物理名だったが、今後はemailに統一する。
- character_1〜character_4はM_CHARACTERS.idへのFK。
- JPAでは既存のCharacter Entityとの@ManyToOne関連として実装する。
- UserCharactersのような中間テーブルは作成しない。
- play_time_start / play_time_endはJavaではLocalTimeとして扱う。
- created_at / updated_atはJavaではLocalDateTimeとして扱う。
- テーブル名は設計上T_USERSと表記するが、JPA/PostgreSQL上の物理テーブル名はt_usersとして扱う。既存のM_CHARACTERS / m_charactersと同じ方針。

# Git Rules

- mainへ直接コミットしない
- featureブランチで作業する
- Claude Codeが勝手にcommitしない
- Claude Codeが勝手にpushしない
- Claude Codeが勝手にmergeしない
- commit / push / mergeは明示的に指示された場合のみ実行する

# AI Development Rules

- 大きな変更の前に関連する既存コードを確認する
- 要件を勝手に追加・変更しない
- 新しいライブラリを勝手に追加しない
- 関係のないファイルを変更しない
- 不明な仕様は推測せず質問する
- 実装後は変更したファイルと内容をまとめて報告する
- 実装後はテスト結果を報告する
- セキュリティ関連の重要な設計変更は実装前に説明する
